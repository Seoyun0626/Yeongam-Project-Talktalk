<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8" />
  <title>Admin Page</title>
  <link rel="stylesheet" href="/css/home/layout.css">
  <link rel="stylesheet" href="/semantic/semantic.min.css">
  <link rel="stylesheet" href="/css/home/dataif/terms.css">
  <script src="/ckeditor/build/ckeditor.js"></script>
  <script> 
    function btn() {
        if (!confirm("설정을 저장하시겠습니까?")) {
            // 현재 페이지로
            alert("취소하였습니다.");
            return false;
        } else {
            alert("저장되었습니다.");
            return true;
        }
    }
   </script> 
</head>
<body>
  <div class='common-background-bar background-bar-terms'></div>
  <div class='common-border-bar border-bar-terms'></div>
  <%- include('../layout.ejs') %>
<section>
    <div>
      <div class="pagename">회원 가입 설정</div>
      <form action="/admin/dataif/terms" method="post">
        <textarea name="terms" id="terms" cols="100" rows="10" ><%= posts[0].terms %></textarea><br>
        <textarea name="privacy" id="privacy" cols="100" rows="10"><%= posts[0].privacy %></textarea><br>
        <input type="submit" value="저장" onclick="return btn()">
      </form>
    </div>
</section>
</div>
</body>

</html>
