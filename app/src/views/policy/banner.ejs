<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8" />
  <title>Admin Page</title>
  <link rel="stylesheet" href="/semantic/layout.css">
  <link rel="stylesheet" href="/semantic/semantic.min.css">
  <link rel="stylesheet" href="/css/home/mem.css">
  <link rel="stylesheet" href="/css/home/admin.css">
  <script src="/ckeditor/build/ckeditor.js"></script>
  <!-- <script src="https://cdn.ckeditor.com/ckeditor5/34.0.0/classic/ckeditor.js"></script> -->
  <!-- <script src="https://cdn.ckeditor.com/ckeditor5/34.0.0/classic/translations/ko.js"></script> -->
  <script src="/js/home/mem.js" defer></script>
</head>
<body>
    <div id='rectanglel4' class='rectanglel4'></div>
    <div id='rectangleb4' class='rectangleb4'></div>
    <%- include('../layout.ejs') %>
<section>
    <div class="pagename">배너 정보</div>
    <table id="banner_table">
        <thead>
            <tr>
                <th>번호</th>
                <th>제목</th>
                <!-- <th>이미지</th> -->
                <th>링크</th>
                <th>등록일</th>
                <th>수정일</th>
                <!-- <th>수정</th> -->
                <th>삭제</th>
            </tr>
        </thead>
        <tbody>
            <% for(var i=0; i<banner.length; i++) { %>
                <tr>
                    <td><%= i+1 %></td>
                    <td><%= banner[i].banner_name %></td>
                    <!-- <td><img src="<%= banner[i].img %>" alt="1"></td> -->
                    <td><%= banner[i].link %></td>
                    <td><%= `${banner[i].ins_date.getFullYear()}` + `-`+ `${banner[i].ins_date.getMonth()+1}` + `-`+ `${banner[i].ins_date.getDate()}`%></td>
                    <td><%= `${banner[i].upd_date.getFullYear()}` + `-`+ `${banner[i].upd_date.getMonth()+1}` + `-`+ `${banner[i].upd_date.getDate()}`%></td>
                    <!-- <td><a href="/admin/policy/banner/<%= banner[i].id %>/edit">수정</a></td> -->
                    <td><a href="/admin/policy/banner/delete/<%= banner[i].board_idx %>">삭제</a></td>
                </tr>
            <% } %>
        </tbody>
    </table>
    <br><br>
    <div class="pagename">배너 등록</div>
    <form action="/admin/policy/banner" method="post" enctype="multipart/form-data">
        <div class="ui form">
            <div class="field">
                <label>제목</label>
                <input type="text" name="name" placeholder="제목을 입력하세요">
            </div>
            <div class="field">
                <label>이미지</label>
                <input type="file" name="img">
            </div>
            <div class="field">
                <label>링크</label>
                <input type="text" name="link" placeholder="링크를 입력하세요">
            </div>
        </div>
        <button class="ui button" type="submit">등록</button>
    </form>
</section>
</body>
</html>